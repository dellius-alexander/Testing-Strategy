//changes view
context('Viewport', () => {
    beforeEach(() => {
      cy.visit('index.html')
    })
  
        it('cy.viewport() - set the viewport size and dimension', () => {
      cy.viewport(900, 600)
      });
it('cy.viewport() - set the viewport size and dimension', () => {
  cy.viewport(50, 600)
});
});
